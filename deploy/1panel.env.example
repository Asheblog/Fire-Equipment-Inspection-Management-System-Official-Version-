# 镜像地址（GitHub Actions 推送到 GHCR）
APP_IMAGE=ghcr.io/<owner>/<repo>:latest

# 对外端口
PORT=3001

# SQLite 默认路径（相对于 backend/prisma）
DATABASE_URL=file:../data/fire_safety.db

# 必填：请替换为强随机密钥
JWT_SECRET=<replace-with-strong-secret>
JWT_REFRESH_SECRET=<replace-with-strong-secret>

# 时区
TZ=Asia/Shanghai
